#include <stdio.h>

/**
 * main - It calls puts() to print to stdout
 *
 * Return: 0 after successful excecution because it's an int type function
 */
int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");

	return (0);
}
