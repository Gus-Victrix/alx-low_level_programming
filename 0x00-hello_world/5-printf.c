#include <stdio.h>

/**
 * main - uses printf to print to stdout
 *
 * Return: 0 after successful excecution
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");

	return (0);
}
